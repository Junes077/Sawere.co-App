Generate real icons before building an installer:

```
npm run tauri icon path/to/sawere-logo.png
```

This populates `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns` and
`icon.ico` as referenced in `tauri.conf.json`.
